
const C=window.UNION_CONFIG||{};document.querySelector('.nav-toggle')?.addEventListener('click',()=>document.querySelector('.navlinks').classList.toggle('open'));const file=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.navlinks a').forEach(a=>{if(a.getAttribute('href')===file)a.classList.add('active')});function toast(m){let t=document.querySelector('.toast');if(!t){t=document.createElement('div');t.className='toast';document.body.appendChild(t)}t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),4000)}
async function submitUnionForm(form,type){const btn=form.querySelector('[type=submit]');btn.disabled=true;const old=btn.textContent;btn.textContent='Sending…';const data=Object.fromEntries(new FormData(form));data.type=type;try{if(!C.API_BASE||C.API_BASE.includes('YOUR-WORKER'))throw new Error('The secure application service has not been configured yet.');const r=await fetch(C.API_BASE+'/api/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error||'Could not send form');form.reset();toast('Submitted successfully. Staff have been notified in Discord.')}catch(e){toast(e.message)}finally{btn.disabled=false;btn.textContent=old}}
document.querySelectorAll('form[data-union-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();submitUnionForm(f,f.dataset.unionForm)}));
