const navbar=document.querySelector('.navbar');const menuButton=document.querySelector('.menu-btn');const navLinks=document.querySelector('.nav-links');window.addEventListener('scroll',()=>{if(!navbar)return;const s=window.scrollY>50;navbar.style.background=s?'rgba(8,5,14,.96)':'rgba(8,5,14,.78)';navbar.style.boxShadow=s?'0 14px 38px rgba(0,0,0,.42)':'none'});menuButton?.addEventListener('click',()=>navLinks?.classList.toggle('open'));navLinks?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>navLinks.classList.remove('open')));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(!t)return;e.preventDefault();t.scrollIntoView({behavior:'smooth',block:'start'})}));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.09});document.querySelectorAll('.card,.support-card,.rule-block,.dashboard-panel,.profile-card').forEach(el=>{el.classList.add('hidden');observer.observe(el)});const topButton=document.createElement('button');topButton.className='topButton';topButton.setAttribute('aria-label','Back to top');topButton.textContent='↑';document.body.appendChild(topButton);window.addEventListener('scroll',()=>topButton.classList.toggle('active',window.scrollY>500));topButton.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
